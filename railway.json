{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install",
    "watchPatterns": ["src/**"]
  },
  "deploy": {
    "startCommand": "npm run server",
    "healthcheckPath": "/api/tokens/top?viewMode=all-time"
  }
}
