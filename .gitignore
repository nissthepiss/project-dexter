
# Wreckit local config (may contain secrets)
.wreckit/config.local.json
node_modules

# Environment variables (may contain secrets)
.env
.env.local
.env.*.local

# Telegram session files (Railway generates its own)
src/data/telegram_credentials.json
src/data/telegram_session-*.txt
src/data/telegram_session*.json
